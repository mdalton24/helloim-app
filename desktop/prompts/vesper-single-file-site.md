<!-- CAPTURED VERBATIM, AND IT IS ONE LINE — THAT IS NOT A CAPTURE FAULT.
     Checked against the session transcript: every newline was already gone
     before this reached us. It survives fine as prose. It does NOT survive as
     code: a `// comment` with no newline after it swallows the statement that
     follows, and this spec is mostly GLSL and JS.
     A reflow was attempted and REVERTED. Splitting on `;` and `{` reindents
     the shader bodies correctly and then silently glues each trailing comment
     onto the next statement — e.g. `// 0->1, orb comes apart orbAlpha(s) = 1 -
     orbDissolve(s);`. The information needed to place those breaks is gone, so
     any reconstruction is a guess that READS correct and is wrong, which is
     worse than the honest single line below.
     TO FIX: get the original .md file, not a paste. -->

# Recreate this site as a single HTML file: Vesper  You are an expert creative front-end developer. Produce a **single self-contained `index.html`** that reproduces the project below **exactly** — same layout, sections, visuals, motion, and interaction. Pure HTML/CSS/JS in one file: no build step, no framework, no bundler. Use ES modules with a CDN importmap for Three.js (r0.185 from unpkg). Hardcode every value given here as a fixed constant. Rebuild each component described below as a section of the one file.  ## What it is  A **scroll-driven WebGL landing page** for *Vesper* — a fictional "living interface" engine. One full-viewport additive particle scene runs behind the whole page and passes through **three forms** as you scroll, each form dissolving into the next:  1. **The orb** — a noise-displaced sphere of ~23,000 points, mint at the top fading to violet at the    bottom, spinning slowly. It bulges and ripples like oil where the cursor touches it. 2. **The galaxy** — the orb blows apart into dust and the dust gathers into a two-arm spiral galaxy    of ~120,000 stars, blue core to mint arms, while the camera dives from z=48 to z=18 and the disc    tips toward edge-on. 3. **The brain** — the galaxy detonates outward and its points converge into a point-cloud **brain**    (~130,000 points area-sampled off a real mesh), same indigo→mint gradient, with white-hot synapse    flashes firing across it and a cyan halo tracking the cursor. It spins as you scroll, then rushes    at the camera and bursts apart as the closing content arrives.  Over that scene, three **fixed** text overlays fade in and out on the same scroll clock (a Figma artboard: huge light display type, uppercase tag copy, a white **Send Request** CTA), then two **white cards** — "A living interface" and a "Frequently asked" accordion — float up over the still- drawing shader, and a transparent footer with a contact pill closes the page. It opens with a black **loader**: a field of angled, colour-graded shooting stars accelerating into warp behind a hairline progress bar counting `000%` → `100%`.  Everything is black, white and the orb's gradient: mint `#52ffa5`, cyan `#6bfdff`, violet `#582eff`, over a near-black backdrop with a slow drifting "flame" domain-warp.  ## Page shell & libraries  - **Three.js r0.185** via an importmap (unpkg), plus these addons from `three/examples/jsm/`:   `postprocessing/EffectComposer`, `postprocessing/RenderPass`, `postprocessing/UnrealBloomPass`.   ```html   <script type="importmap">   {"imports":{     "three":"https://unpkg.com/three@0.185.0/build/three.module.js",     "three/addons/":"https://unpkg.com/three@0.185.0/examples/jsm/"   }}   </script>   ``` - **Smooth scroll: Lenis 1.3.19** — `https://unpkg.com/lenis@1.3.19/dist/lenis.min.js`, options   `{ smoothWheel: true }`. Drive `lenis.raf(time)` from **the same** `requestAnimationFrame` loop that   drives everything else, and call it **first** in the frame — Lenis *writes* the scroll position and   every scroll-driven value below *reads* it back; in the other order the whole page trails the scroll   by one frame. - **Fonts.** Two faces:   - **General Sans** (display + body) — Fontshare:     `https://api.fontshare.com/v2/css?f[]=general-sans@300,400,500&display=swap`. Used at weight 300     (light) for every display heading.   - **Mulish** (uppercase tags, stats, loader chrome) — Google Fonts:     `https://fonts.googleapis.com/css2?family=Mulish:wght@300;400&display=swap`. - No router, no cookie/consent UI, no SEO plumbing, no contact API, no analytics — drop all of it.   The contact form's submit is a stub (`event.preventDefault()`).  ### The vw scaling model (bake this in)  The desktop design is a **1440×800 artboard**, and every desktop measurement below is that artboard's pixel value **÷ 14.4**, expressed in `vw` (so 16px → `1.111vw`, 80px → `5.556vw`). The page is one artboard scaled proportionally: at any desktop width it stays the same composition. Copy the `vw` values given here literally.  Root font-size ladder (rem-based spacing scales with the viewport, then stops below 1024px, where the layout is no longer a scaled copy of the artboard but a purpose-built stack in real pixels):  ```css html { font-size: 16px; -webkit-font-smoothing: antialiased; } @media (max-width: 1920px)   { html { font-size: 0.833333vw; } }  /* 16*100/1920 */ @media (max-width: 1440px)   { html { font-size: 1.111111vw; } }  /* 16*100/1440 */ @media (max-width: 1023.98px){ html { font-size: 16px; } }        /* rem means real px again */  html, body { overflow-x: clip; }        /* nothing may scroll the page sideways */ body { background:#170a2b; color:#fdfdfd; overscroll-behavior-y:none; } body::-webkit-scrollbar { width: 0; } ```  ### Design tokens (exact)  ```css :root{   --background:#170a2b;   /* deep violet page bg — only ever seen for one frame */   --foreground:#fdfdfd;   --void:#170a2b;   --chalk:#fdfdfd;   --signal:#6cf3a3;       /* accent / focus ring / hover ink */   --surface:#100823;   --surface-raised:#17102e; } ```  Typography: display headings are **General Sans 300**, `font-size:5.556vw`, `line-height:0.9`. Tag/label copy is **Mulish**, `font-size:1.111vw`, `line-height:1.2`, `text-transform:uppercase`. Body copy is **General Sans 400**, `1.111vw`/`1.2`.  ## Text reveal system (build this once, use it everywhere)  Every heading and paragraph animates in. Write two small helpers — no library:  - **`splitLetters(el)`** — wrap each character of the element's text in a `<span>` (preserve spaces as   `&nbsp;`-width gaps). Container: `display:flex; flex-wrap:wrap; column-gap:0.25em`, words kept   together so they wrap as words, not letters. - **`splitWords(el)`** — same, one span per word, each span wrapping an **inner** span (translate the   inner one so the outer box never moves).  Three presets. Apply the "out" state initially, then transition to the "in" state with a per-index stagger:  ```js LETTER_REVEAL = { out:{opacity:0, filter:'blur(12px)'},              in:{opacity:1, filter:'blur(0px)'},                   stagger:26,  duration:1000, easing:'cubic-bezier(0.23,1,0.32,1)' }  /* easeOutQuint */ WORD_REVEAL   = { out:{opacity:0, filter:'blur(8px)',  y:16},        in:{opacity:1, filter:'blur(0px)', y:0},                   stagger:42,  duration:1200, easing:'cubic-bezier(0.23,1,0.32,1)' } UNIT_REVEAL   = { out:{opacity:0, filter:'blur(10px)', y:20},        in:{opacity:1, filter:'blur(0px)', y:0},                   duration:1000, easing:'cubic-bezier(0.23,1,0.32,1)' }   /* buttons, tag rows, labels */ MASK_REVEAL   = { out:{clipPath:'inset(0 0 100% 0)'},                in:{clipPath:'inset(0 0 0% 0)'},                   duration:1200 }                                          /* the photo, opens downward */ CARD_REVEAL   = { out:{opacity:0, transform:'scale(0.94) rotateX(12deg)', filter:'blur(14px)'},                   in:{opacity:1,  transform:'scale(1) rotateX(0deg)',     filter:'blur(0px)'},                   duration:1200 }                                          /* the two white cards */ ```  Letters carry **no transform** (opacity + blur only) so the text box never shifts; words translate via their inner span. `CARD_REVEAL` needs `perspective:1400px` on the card's wrapper and `transform-origin:bottom` on the card, so the tilt reads in 3D.  Two trigger modes: - **`once`** — plays when it first becomes eligible and never reverses (hero copy, footer). - **`always`** — plays in when its trigger is active and **reverses out** when it leaves (the fixed   section overlays, the two white cards).  Fixed overlays cannot use IntersectionObserver (they never leave the viewport), so they gate on the **scroll clock window** given per-section below. The in-flow cards use IntersectionObserver.  ## The scroll clock (the spine of the whole page)  A global clock runs **0 → 4**. It is driven by four invisible full-height tracks in the document, and *everything* — the scene, the overlays, the camera — reads it.  **The document.** After the fixed overlays, in order:  ```html <main>   <!-- fixed: header, loader, hero overlay, galaxy overlay, brain overlay, canvas -->   <div aria-hidden style="pointer-events:none">     <div id="track1" style="height:200lvh"></div>     <div id="track2" style="height:200lvh"></div>     <div id="track3" style="height:200lvh"></div>     <div style="height:100lvh"></div>   <!-- trailing space, not a track -->   </div>   <div style="position:relative; z-index:10">     <section id="financial" style="margin-bottom:1.667vw"> … white card … </section>     <section id="faq"> … white card … </section>     <footer> … </footer>   </div> </main> ```  One track = **200lvh** (`SCROLL_TRACK_LVH = 200`) — deliberately longer than a viewport, so the forms need real scroll to hand off.  **The four track progresses**, with `T = 2 * innerHeight` (one track) and `vh = innerHeight`:  ```js const clamp01 = x => Math.min(Math.max(x, 0), 1); p1 = clamp01( scrollY / T );                        // track1: top→top p2 = clamp01( (scrollY - T) / T );                  // track2: top→top p3 = clamp01( (scrollY - 2*T) / T );                // track3: top→top p4 = clamp01( (scrollY + vh - 2*T) / T );           // track3 AGAIN, measured against the viewport BOTTOM target = p1 + p2 + p3 + p4;                         // 0 → 4 ```  That fourth reading is not a mistake: measuring track 3 against the viewport *bottom* makes it resolve like track 2, so the clock is effectively `p1 + 2·p2 + p3` and **runs at double rate through its middle**. The keyframes below were authored against that shape — keep it.  **Easing.** The clock does not snap to the scroll position; it eases toward it, frame-rate independently (`0.07` is the fraction closed per 1/60 s — a ~0.25 s time constant, so the scene trails the scroll a beat and the hand-offs glide):  ```js let progress = 0; function advance(dt){                                // dt in seconds, capped at 0.05   const d = target - progress;   if (Math.abs(d) < 0.00005) { progress = target; return; }   progress += d * (1 - Math.pow(1 - 0.07, dt * 60)); // 0.07 desktop; see the adaptive table } ```  **Scene state** — five keyframes at clock 0,1,2,3,4, linearly interpolated:  ```js const KEYFRAMES = [   { orbOut:0, galaxyIn:0, galaxyOut:0,   galaxyDive:0   },   { orbOut:1, galaxyIn:1, galaxyOut:0,   galaxyDive:0.5 },   { orbOut:1, galaxyIn:1, galaxyOut:0.5, galaxyDive:1   },   { orbOut:1, galaxyIn:1, galaxyOut:1,   galaxyDive:1   },   { orbOut:1, galaxyIn:1, galaxyOut:1,   galaxyDive:1   }, ]; // sampleScene(g): clamp g to [0,4], lower = min(floor(g), 3), t = g - lower, lerp each key. ```  **Derived curves** — copy exactly:  ```js const smoothstep   = (a,b,x) => { const t = clamp01((x-a)/(b-a)); return t*t*(3-2*t); }; const smootherstep = x => { const t = clamp01(x); return t*t*t*(t*(t*6-15)+10); };  orbDissolve(s)     = smootherstep(s.orbOut);                       // 0→1, orb comes apart orbAlpha(s)        = 1 - orbDissolve(s); orbCoreOpen(g)     = smoothstep(2.9, 3.6, g);                      // bores a hole through the orb orbApproach(g)     = smoothstep(3.4, 4.0, g);                      // orb rushes the camera galaxyAppear(s)    = smootherstep(smoothstep(0.55, 0.95, s.galaxyIn)); galaxyAssemble(s)  = 1 - smootherstep(smoothstep(0.50, 1.00, s.galaxyIn));  // 1 = still inbound galaxyBlow(s)      = smootherstep(clamp01((s.galaxyOut - 0.5) * 2)); galaxyPresence(s)  = galaxyAppear(s) * (1 - galaxyBlow(s));        // how much of the frame the galaxy owns brainAppear(g)     = smootherstep(smoothstep(2.6, 3.4, g));        // brain gathers as the galaxy blows ```  **The intro clock** — a single 0→1 value, started when the loader curtain lifts, duration **2900 ms**, easing `easeOutCubic`. Drives the camera dolly-in, the orb's assembly, the hero line, and the hero copy.  **The outro clock** — 0→1, scrubbed by the **Financial card**'s arrival: trigger start `top bottom`, end `top top`, i.e. `outro = clamp01((scrollY + vh - financialTop) / vh)`. It spans a **full viewport** of scroll so the brain's exit stays gradual. The brain plays its burst against it; the orb dims out on it. The scene keeps drawing — the shader stays visible around the inset cards.  ## The WebGL scene  One full-viewport canvas, **fixed**, `pointer-events:none`, behind everything (`inset:0`).  ```js renderer = new THREE.WebGLRenderer({ alpha:true, antialias:false, powerPreference:'high-performance' }); renderer.setPixelRatio(Math.min(devicePixelRatio, 2));   // fill-rate bound: cost ∝ dpr² renderer.toneMapping = THREE.ACESFilmicToneMapping; renderer.toneMappingExposure = 1; renderer.outputColorSpace = THREE.SRGBColorSpace; scene.background = new THREE.Color('#000000');  camera = new THREE.PerspectiveCamera(50, w/h, 0.1, 400); camera.position.set(0, 0, 3.8 + 10);                     // orbCameraZ + INTRO_DOLLY ```  **Colour is linear.** The renderer encodes linear → sRGB on output, so every colour handed to a shader must be converted first. Uploading raw hex bytes double-encodes and washes everything out. Use:  ```js const linear = hex => { const c = new THREE.Color(hex); return new THREE.Vector3(c.r, c.g, c.b); }; ``` (`THREE.Color` does the sRGB→linear conversion on construction.) Every `uCol*` uniform below is a `linear(hex)` vec3.  **Draw order.** Backdrop (clip-space quad, `renderOrder -1`), hero line (`renderOrder -0.5`), then the three additive point forms, then the camera-attached motes. All point materials: `transparent:true, depthWrite:false, blending:THREE.AdditiveBlending` (orb and motes also `depthTest:false`).  ### Shared 3D simplex noise (used by the orb and the backdrop)  ```glsl vec3 mod289(vec3 x){return x - floor(x*(1.0/289.0))*289.0;} vec4 mod289(vec4 x){return x - floor(x*(1.0/289.0))*289.0;} vec4 permute(vec4 x){return mod289(((x*34.0)+1.0)*x);} vec4 taylorInvSqrt(vec4 r){return 1.79284291400159 - 0.85373472095314*r;} float snoise(vec3 v){   const vec2 C = vec2(1.0/6.0, 1.0/3.0);   const vec4 D = vec4(0.0, 0.5, 1.0, 2.0);   vec3 i = floor(v + dot(v, C.yyy));   vec3 x0 = v - i + dot(i, C.xxx);   vec3 g = step(x0.yzx, x0.xyz);   vec3 l = 1.0 - g;   vec3 i1 = min(g.xyz, l.zxy);   vec3 i2 = max(g.xyz, l.zxy);   vec3 x1 = x0 - i1 + C.xxx;   vec3 x2 = x0 - i2 + C.yyy;   vec3 x3 = x0 - D.yyy;   i = mod289(i);   vec4 p = permute(permute(permute(       i.z + vec4(0.0, i1.z, i2.z, 1.0))       + i.y + vec4(0.0, i1.y, i2.y, 1.0))       + i.x + vec4(0.0, i1.x, i2.x, 1.0));   float n_ = 0.142857142857;   vec3 ns = n_ * D.wyz - D.xzx;   vec4 j = p - 49.0 * floor(p * ns.z * ns.z);   vec4 x_ = floor(j * ns.z);   vec4 y_ = floor(j - 7.0 * x_);   vec4 x = x_ * ns.x + ns.yyyy;   vec4 y = y_ * ns.x + ns.yyyy;   vec4 h = 1.0 - abs(x) - abs(y);   vec4 b0 = vec4(x.xy, y.xy);   vec4 b1 = vec4(x.zw, y.zw);   vec4 s0 = floor(b0)*2.0 + 1.0;   vec4 s1 = floor(b1)*2.0 + 1.0;   vec4 sh = -step(h, vec4(0.0));   vec4 a0 = b0.xzyw + s0.xzyw*sh.xxyy;   vec4 a1 = b1.xzyw + s1.xzyw*sh.zzww;   vec3 p0 = vec3(a0.xy, h.x);   vec3 p1 = vec3(a0.zw, h.y);   vec3 p2 = vec3(a1.xy, h.z);   vec3 p3 = vec3(a1.zw, h.w);   vec4 norm = taylorInvSqrt(vec4(dot(p0,p0), dot(p1,p1), dot(p2,p2), dot(p3,p3)));   p0 *= norm.x; p1 *= norm.y; p2 *= norm.z; p3 *= norm.w;   vec4 m = max(0.6 - vec4(dot(x0,x0), dot(x1,x1), dot(x2,x2), dot(x3,x3)), 0.0);   m = m * m;   return 42.0 * dot(m*m, vec4(dot(p0,x0), dot(p1,x1), dot(p2,x2), dot(p3,x3))); } ```  And the screen-space domain warp (backdrop):  ```glsl vec3 warp3d(vec3 pos, float t){   float curv = 0.8, a = 1.9, b = 0.7;   pos *= 2.0;   pos.x += curv*sin(t + a*pos.y) + t*b; pos.y += curv*cos(t + a*pos.x);   pos.y += curv*sin(t + a*pos.z) + t*b; pos.z += curv*cos(t + a*pos.y);   pos.z += curv*sin(t + a*pos.x) + t*b; pos.x += curv*cos(t + a*pos.z);   return 0.5 + 0.5*cos(pos.xyz + vec3(1, 2, 4)); } ```  ### 1. Backdrop — full-screen flame wash  A `PlaneGeometry(2,2)` whose vertex shader writes **clip space directly**, so it ignores the camera and always covers the viewport. `depthTest:false, depthWrite:false, toneMapped:false`, `renderOrder:-1`.  ```glsl // vertex varying vec2 vUv; void main(){ vUv = uv; gl_Position = vec4(position, 1.0); } ``` ```glsl // fragment uniform float iTime; uniform vec3  uBg; uniform vec3  uFlameA; uniform vec3  uFlameB; uniform float uFlameAmt; varying vec2 vUv; // ...warp3d here... void main(){   vec2 uv = 2.0 * vUv - 1.0;   vec3 w = pow(warp3d(vec3(uv.x, sin(uv.y), uv.y), iTime * 1.5), vec3(1.5));   vec3 flame = 1.5 * uFlameA * w.x;   flame *= w.y;   flame += uFlameB * w.z;   flame *= smoothstep(0.25, 1.0, abs(uv.y));   float md = smoothstep(-0.7, 1.0, -uv.y * uv.x);   flame *= md * md;   vec3 bg = uBg * (1.0 - 0.4 * length(uv));   gl_FragColor = vec4(bg + flame * uFlameAmt, 1.0); } ```  Its palette **crossfades on `galaxyPresence`** between the orb's and the galaxy's tuning:  | uniform | orb | galaxy | |---|---|---| | `uBg` | `#000000` | `#000000` | | `uFlameA` | `#8b98fe` | `#8b98fe` | | `uFlameB` | `#6bfdff` | `#6bfdff` | | `uFlameAmt` | `0.5` | `0.195` |  Per frame: `iTime = elapsed`, and `lerpVectors(orb, galaxy, galaxyPresence)` for each colour, plus the scalar `flameAmt`.  ### 2. Hero line — the rule behind the orb  A second clip-space quad, `renderOrder:-0.5`, drawn after the backdrop but **before** the particles, so the additive orb draws over it. Additive points can't occlude, so the line **fades out across the orb's central footprint** to read as genuinely behind it.  ```glsl // fragment (vertex is the same clip-space passthrough as the backdrop) uniform float uY, uThickness, uMaskRadius, uAlpha; varying vec2 vUv; void main(){   float d = abs(vUv.y - uY);   float line = 1.0 - smoothstep(0.0, uThickness, d);   float side = smoothstep(uMaskRadius * 0.6, uMaskRadius, abs(vUv.x - 0.5));   gl_FragColor = vec4(vec3(1.0), line * side * 0.2 * uAlpha);   // white @ 20% } ``` Per frame (design width 1440, line 264px above the artboard bottom): ```js const fromBottomPx = 264 * width / 1440; uY          = clamp(fromBottomPx / height, 0.02, 0.95); uThickness  = 0.6 / height;                    // crisp ~1px uMaskRadius = 0.36 * height / width;           // the orb's horizontal half-extent uAlpha      = clamp01((intro - 0.35)/0.65) * (1 - clamp01((clock - 0.08)/0.2)); ```  ### 3. The orb — hero form  **Geometry:** a **Fibonacci sphere**, radius 1, `orbCount` points (23,000 desktop), plus a `aRandom` vec3 attribute of `Math.random() - 0.5` per point:  ```js const golden = Math.PI * (3 - Math.sqrt(5)); for (let i = 0; i < count; i++) {   const y = 1 - (i / (count - 1)) * 2;   const r = Math.sqrt(Math.max(0, 1 - y*y));   const th = golden * i;   pos[i*3] = Math.cos(th) * r * radius;  pos[i*3+1] = y * radius;  pos[i*3+2] = Math.sin(th) * r * radius; } ```  **Vertex shader** (verbatim — `snoise` prepended):  ```glsl uniform float uTime, uSize, uPR, uDeform, uOut, uAssemble, uCore; uniform vec3  uCentre, uCamLocal, uCursor, uCursorVel; uniform float uEnergy, uPointerRadius, uOilBulge, uOilRipple, uOilDrag, uRippleFreq, uRippleSpeed; attribute vec3 aRandom; varying float vAlpha, vLit, vVert, vSide, vOil; // ...snoise here... void main(){   vec3 p = position;    // Soft radial deformation — two noise octaves, drifting over time.   float n1 = snoise(p * 1.6 + vec3(0.0, uTime * 0.18, 0.0));   float n2 = snoise(p * 3.3 - vec3(uTime * 0.12));   float disp = n1 * 0.72 + n2 * 0.28;   vec3 pos = p * (1.0 + uDeform * disp);    float lit = smoothstep(-0.25, 0.5, disp);   vLit = lit;    // CORE: a bore straight through the blob, along the view axis, in OBJECT space   // so the rim rides the displaced surface instead of being stamped over it.   vec3 axis = normalize(uCamLocal);   float axial = dot(pos, axis);   float radial = length(pos - axis * axial);   float bore = mix(0.36, 0.56, uCore) * (1.0 + disp * 0.55);   float hole = smoothstep(bore, bore + 0.10, radial);    // REVEAL: stream in from the camera, each point on its own delay.   float delay = (aRandom.z + 0.5) * 0.55;   float t = clamp((uAssemble - delay) / (1.0 - delay), 0.0, 1.0);   float ease = 1.0 - pow(1.0 - t, 3.0);   vec3 spawn = uCamLocal * 0.92 + vec3(aRandom.x * 1.9, -1.7 + aRandom.y * 1.3, 0.0);   pos = mix(spawn, pos, ease);   float assembleFade = ease;    // DISSOLVE: liquid sheets riding outward.   float out2 = uOut * uOut;   vec3 flow = normalize(p + aRandom * 0.4);   float sheet = snoise(p * 1.9 + vec3(uTime * 0.25));   float ripple = sin(length(p) * 6.0 - uTime * 1.6 + sheet * 3.0);   float travel = out2 * (9.0 + sheet * 5.0 + ripple * 1.6);   pos += flow * travel;   pos += vec3(sheet, ripple, sheet * ripple) * uOut * 0.55;    // OIL: viscous cursor disturbance, fixed in WORLD space (does not spin with the orb).   vec4 wpos = modelMatrix * vec4(pos, 1.0);   vec3 wN = normalize(wpos.xyz - uCentre);   float ang = acos(clamp(dot(wN, normalize(uCursor)), -1.0, 1.0));   float fall = smoothstep(uPointerRadius, 0.0, ang);   float bulge = fall * fall;   float oilRipple = sin(ang * uRippleFreq - uTime * uRippleSpeed) * fall;   float oil = (bulge * uOilBulge + oilRipple * uOilRipple) * uEnergy;   wpos.xyz += wN * oil;   wpos.xyz += uCursorVel * fall * uOilDrag * uEnergy;   vOil = (bulge + max(oilRipple, 0.0) * 0.5) * uEnergy;    vec4 mv = viewMatrix * wpos;   gl_Position = projectionMatrix * mv;   gl_PointSize = uSize * uPR * (1.0 / max(0.1, -mv.z));    vVert = mv.y;          // view-space colouring: gradient stays fixed to the screen   vSide = abs(mv.x);    float depth = pos.z * 0.5 + 0.5;   float outFade = 1.0 - smoothstep(0.0, 0.92, uOut);   // Alpha floors: additive points can only ADD light, but a point whose alpha collapses   // reveals the near-black backdrop and reads as a black blotch. Keep floors under both terms.   vAlpha = hole * (0.28 + 0.32 * depth) * (0.38 + 0.72 * lit) * outFade * assembleFade; } ```  **Fragment shader** (verbatim):  ```glsl precision highp float; uniform vec3  uColTop, uColBottom, uColEdge; uniform float uBrightness, uOpacity, uAppear, uIri; varying float vAlpha, vLit, vVert, vSide, vOil; void main(){   vec2 uv = gl_PointCoord - 0.5;   float d = length(uv);   if (d > 0.5) discard;   // Tight bright core + thin halo — wide soft blobs overlapping additively read "soapy".   float soft = smoothstep(0.5, 0.0, d);   soft = soft * soft * 1.2;   float core = smoothstep(0.13, 0.0, d);    float vt = clamp(vVert * 0.5 + 0.5, 0.0, 1.0);   vec3 col = mix(uColBottom, uColTop, vt);   col = mix(col, uColEdge, smoothstep(0.72, 1.15, vSide));   col *= (0.78 + 0.42 * vLit);   col += smoothstep(0.6, 1.0, vLit) * core * vec3(0.45);    float cap = smoothstep(0.55, 1.05, abs(vVert));   // extra glow at the poles; bloom picks it up   col += col * cap * 1.1;   float capA = 1.0 + 0.6 * cap;    // Oil slick: thin-film iridescence over the disturbed area.   float oilMask = clamp(vOil, 0.0, 1.0);   vec3 iri = 0.5 + 0.5 * cos(6.2831853 * (vOil * 2.4 + abs(vVert) * 0.3 + vec3(0.0, 0.33, 0.67)));   col = mix(col, iri, oilMask * uIri);   col += oilMask * 0.3;    col *= uBrightness;   gl_FragColor = vec4(col, soft * vAlpha * capA * uOpacity * uAppear * (1.0 + oilMask * 0.5)); } ```  **Per frame:** ```js uTime = elapsed;  uPR = renderer.getPixelRatio(); uAppear   = 1 - outro;                       // hand the frame to the closing sections uAssemble = intro;                           // 0→1 over the 2900ms intro uOut      = orbDissolve(state); uCore     = orbCoreOpen(clock); points.rotation.y = elapsed * 0.17;                       // spin points.rotation.x = Math.sin(elapsed * 0.1) * 0.39;       // tilt wobble const approach = orbApproach(clock); group.position.z = 0.8 * 2.6 * approach;                  // orbMoveY * ORB.approach — rushes the camera uDeform = 0.135 * (1 + approach * 3.4);                   // and its noise swells as it comes // The bore and the spawn origin are object-space and the points spin, so recompute the camera // in the points' local space AFTER the rotation, from a fresh world matrix: points.updateMatrixWorld(); uCamLocal = points.worldToLocal(camera.position.clone()); uCentre   = new THREE.Vector3(0, group.position.y, 0); ``` Visible only while `intro > 0.001 && (1-outro) > 0.002 && orbAlpha(state) > 0.002`.  **The oil pointer** (the orb's signature interaction) — ray-cast the cursor onto the orb's sphere, lag the contact point (viscosity), and accumulate an energy that builds with motion and decays slowly:  ```js function createOilPointer(radius){   const smooth = new THREE.Vector3(0,0,radius), prev = smooth.clone();   const velocity = new THREE.Vector3(), rawVel = new THREE.Vector3();   const pointer = new THREE.Vector2();          // eased NDC pointer   let energy = 0, idle = 0;   return {     cursor: smooth, velocity, get energy(){ return energy; },     step(camera, pointerNdc, centre, enabled, delta){       const ease = base => 1 - Math.pow(1 - base, delta * 60);   // frame-rate independent       pointer.lerp(pointerNdc, ease(0.11));       const target = smooth.clone();       if (enabled) {         const ndc = new THREE.Vector3(pointer.x, pointer.y, 0.5).unproject(camera);         const dir = ndc.sub(camera.position).normalize();         const o2c = camera.position.clone().sub(centre);         const b = 2 * dir.dot(o2c);         const c = o2c.lengthSq() - radius * radius;         const disc = b*b - 4*c;         if (disc >= 0) {                                   // near hit on the front hemisphere           const t = (-b - Math.sqrt(disc)) / 2;           target.copy(o2c).addScaledVector(dir, t);         } else {                                           // miss: closest point on the ray, pushed onto the surface           const tc = Math.max(0, -dir.dot(o2c));           target.copy(o2c).addScaledVector(dir, tc).normalize().multiplyScalar(radius);         }       }       prev.copy(smooth);       smooth.lerp(target, ease(0.1));                      // viscous lag — glides across the silhouette       rawVel.subVectors(smooth, prev).multiplyScalar(2.5);       const radial = smooth.clone().normalize();       rawVel.addScaledVector(radial, -rawVel.dot(radial)); // tangential smear only       if (rawVel.length() > 0.6) rawVel.setLength(0.6);       velocity.lerp(rawVel, ease(0.16));                   // low-pass, or the bulge twitches       const speed = smooth.distanceTo(prev);       const drive = Math.min(Math.max(speed * 8, 0), 1);       if (drive > energy) energy += (drive - energy) * ease(0.08);   // soft attack       else energy *= Math.pow(0.97, delta * 60);                     // slow release       idle = speed < 0.0005 ? idle + delta : 0;       if (!enabled || idle > 2.5) energy *= Math.pow(0.9, delta * 60);     },   }; } ``` All its vectors are **centre-relative**; the shader normalises `uCursor` to get a surface direction.  ### 4. The galaxy — second form  **Geometry:** a `SphereGeometry(4.2, 160, 420)` used **only as a seed lattice** — the vertex shader hashes each position into an arm or bulge coordinate; it is never drawn as a sphere. Critically: **drop the index** (`sphere.setIndex(null)`). `THREE.Points` honours the index, so the indexed sphere would draw ~6× duplicate sprites stacked exactly on top of each other. Additive blending is linear, so multiply opacity by the duplicate factor to reproduce the same image at a sixth of the work:  ```js const vertices = sphere.attributes.position.count; const duplicates = sphere.index ? sphere.index.count / vertices : 1; sphere.setIndex(null); const opacity = 0.55 * duplicates;      // GALAXY.opacity * duplicates ```  **Vertex shader** (verbatim):  ```glsl uniform float uTime, uSize, uArmSpin, uScale, uBlow, uAssemble; uniform vec3  uColEdge, uColCore, uCursor; uniform float uRepelRadius, uRepelStrength, uActivity; varying float vFade; varying vec3  vColor; void main() {   float gRnd1 = fract(sin(dot(position.xyz, vec3(12.989, 78.233, 45.164))) * 43758.545);   float gRnd2 = fract(sin(dot(position.xyz, vec3(93.989, 67.345, 54.256))) * 24634.634);   float gRnd3 = fract(sin(dot(position.xyz, vec3(43.332, 11.235, 89.234))) * 56475.234);   float gRnd4 = fract(sin(dot(position.xyz, vec3(75.321, 32.123, 23.456))) * 35432.123);    float galaxyR = pow(gRnd1, 2.0) * 60.0 + pow(gRnd2, 3.0) * 30.0;   float swirl = pow(galaxyR, 1.1) * 0.08;   float armIndex = floor(gRnd2 * 2.0);   float baseAngle = armIndex * 3.14159265;   float uniformTheta = gRnd3 * 2.0 - 1.0;   float dTheta = pow(uniformTheta, 5.0) * 3.14159;   float theta = baseAngle + swirl + dTheta + uTime * uArmSpin;    float gx = galaxyR * cos(theta);   float gz = galaxyR * sin(theta);   float gyDisc = pow(gRnd4 * 2.0 - 1.0, 3.0) * 1.5;   vec3 basePos = vec3(gx, gyDisc, gz);    float bulgeStrength = smoothstep(25.0, 0.0, galaxyR);   float gRnd5 = fract(sin(gRnd1 * 44.44 + gRnd2) * 555.55);   float gRnd6 = fract(sin(gRnd3 * 66.66 + gRnd4) * 777.77);   float gRnd7 = fract(sin(gRnd5 * 88.88 + gRnd6) * 999.99);   float phi = acos(gRnd5 * 2.0 - 1.0);   float thetaS = gRnd6 * 6.2831853 + uTime * (uArmSpin * 2.0);   float rS = pow(gRnd7, 2.0) * 18.0;   vec3 bulge = vec3(rS * sin(phi) * cos(thetaS), rS * cos(phi), rS * sin(phi) * sin(thetaS));    vec3 galaxyPos = mix(basePos, bulge, bulgeStrength);   vec3 blowDir = normalize(vec3(gRnd1, gRnd2, gRnd3) - 0.5 + 0.0001);    // Assembly — each star streams in from far out, on its own delay.   float delay = gRnd4 * 0.45;   float at = clamp((1.0 - uAssemble - delay) / (1.0 - delay), 0.0, 1.0);   float aEase = 1.0 - pow(1.0 - at, 3.0);   vec3 spawn = galaxyPos + blowDir * 70.0 + vec3(0.0, (gRnd3 - 0.5) * 26.0, 0.0);   galaxyPos = mix(spawn, galaxyPos, aEase);    // Dispersal — every point flies out along its own direction.   galaxyPos += blowDir * uBlow * 90.0;    vec3 finalPos = galaxyPos * uScale;   vec4 modelPosition = modelMatrix * vec4(finalPos, 1.0);    vec3 toP = modelPosition.xyz - uCursor;                 // cursor void   float cd = length(toP);   float fall = smoothstep(uRepelRadius, 0.0, cd);   modelPosition.xyz += normalize(toP + vec3(0.0001)) * fall * uRepelStrength * uActivity;   vec4 mvPosition = viewMatrix * modelPosition;    float coreMix = smoothstep(80.0, 0.0, galaxyR);   vColor = mix(uColEdge, uColCore, clamp(coreMix, 0.0, 1.0));    float isOrb = step(0.98, fract(gRnd1 * 77.77));         // 2% of stars are big   float starSize = mix(1.0, 3.0, isOrb);   vFade = mix(0.7, 1.0, isOrb);    gl_PointSize = uSize * starSize * (10.0 / -mvPosition.z);   gl_PointSize = max(gl_PointSize, 1.5);   gl_Position = projectionMatrix * mvPosition; } ```  **Fragment shader** (verbatim):  ```glsl uniform float uOpacity, uBrightness, uAppear, uFade; varying float vFade; varying vec3  vColor; void main() {   vec2 xy = gl_PointCoord - 0.5;   float ll = length(xy);   if (ll > 0.5) discard;   float a = smoothstep(0.5, 0.1, ll);   gl_FragColor = vec4(vColor * uBrightness, vFade * a * uOpacity * uAppear * uFade); } ```  **Per frame:** ```js uTime = elapsed; uAppear   = galaxyAppear(state); uBlow     = galaxyBlow(state); uFade     = 1 - galaxyBlow(state); uAssemble = galaxyAssemble(state); group.rotation.x = -(0.5 + state.galaxyDive * 0.5);     // -(|tilt| + dive*diveTilt) — tips edge-on group.rotation.z = pointer.x * 0.12; // Cursor void: project the pointer onto the z=0 plane in WORLD space, then ease toward it. cursorTarget = <ray/plane intersection at z=0>;  cursor.lerp(cursorTarget, 0.12); activity += (pointerReaction ? 1 : 0 - activity) * Math.min(1, delta * 4); ``` Visible while `(appear > 0.001 || assemble < 0.999) && blow < 0.999` — the stars are already streaming in while `appear` is still small, so gate on assembly too or the inbound dust is invisible.  ### 5. The brain — third form  Loads a **baked mesh** (`brain-mesh.bin`, 43 KB — see Assets) and area-samples `brainCount` points across its surface. **Fetch it, never block on it** — it is not needed until the third act.  **The container format** (`VBRN`, little-endian): `u32 magic = 0x4e524256`, `u32 version = 1`, `u32 vertexCount`, `u32 indexCount`, then `vertexCount*3` **Int16** positions (each `/32767` of the unit radius), then `indexCount` **Uint16** indices. It arrives already centred on its surface centroid and scaled to a unit bounding radius.  ```js const decodeBrainMesh = (buffer) => {   const view = new DataView(buffer);   if (view.getUint32(0, true) !== 0x4e524256) throw new Error('bad magic');   const vertexCount = view.getUint32(8, true);   const indexCount  = view.getUint32(12, true);   const q = new Int16Array(buffer, 16, vertexCount * 3);   const positions = new Float32Array(vertexCount * 3);   for (let i = 0; i < positions.length; i++) positions[i] = q[i] / 32767;   const indexBase = 16 + vertexCount * 3 * 2;   const indices = new Uint16Array(buffer.slice(indexBase, indexBase + indexCount * 2));   return { positions, indices }; }; ```  **Area-weighted surface sampling** — build a cumulative-area table over the triangles so a uniform draw picks a triangle in proportion to the surface it owns (otherwise dense clusters of tiny triangles get oversampled and the cloud clumps). Per sample: pick a triangle by binary search on the CDF, take a uniform barycentric point (fold the far half of the unit square back over the diagonal), scale by `radius = 1.15`, and store the triangle's geometric normal plus a random `aSeed`. Attributes: `position` (vec3), `aNormal` (vec3), `aSeed` (float), `aOcclusion` (float, all zeros — `uOcclusionStrength` is 0 to match; keep the attribute so the layout stays intact).  **Vertex shader** (verbatim):  ```glsl attribute float aSeed; attribute float aOcclusion; attribute vec3 aNormal; uniform float iTime; uniform float iResolutionY; uniform float uSize; uniform float uSynapseRate; uniform float uCenterRadius; uniform float uFlowSpeed; uniform float uFlowAmount; uniform vec3 uHighlightPos; uniform float uHighlightRadius; uniform float uHighlightStrength; uniform float uExplode; uniform float uExplodeDist; uniform vec2 uMouse; uniform float uCursor; uniform float uAspect; uniform float uCursorRadius; varying float vSeed; varying float vSynapse; varying float vHemi; varying float vDepth; varying float vFrontness; varying float vCenterness; varying float vOcclusion; varying float vHighlight; varying float vFar; varying float vCursor; varying vec3 vWorldPos; void main() {   vSeed = aSeed; vOcclusion = aOcclusion;   vec3 p = position; vWorldPos = p; vHemi = step(0.0, p.x);   vHighlight = (1.0 - smoothstep(0.0, uHighlightRadius, distance(position, uHighlightPos))) * uHighlightStrength;   vec3 focalDir = normalize(uHighlightPos + vec3(1e-5));   float align = dot(normalize(position + vec3(1e-5)), focalDir);   vFar = smoothstep(0.55, -0.35, align);   vec3 rad = normalize(p + vec3(1e-5));   float breathe = sin(iTime * 1.6 + aSeed * 6.0) * 0.012;   p += rad * breathe;   vec3 nrm = normalize(aNormal + vec3(1e-5));   vec3 ref = abs(nrm.y) < 0.95 ? vec3(0.0, 1.0, 0.0) : vec3(1.0, 0.0, 0.0);   vec3 tA = normalize(cross(nrm, ref));   vec3 tB = cross(nrm, tA);   float ph = iTime * uFlowSpeed + aSeed * 6.2831;   vec3 loopDir = tA * cos(ph) + tB * sin(ph);   p += loopDir * uFlowAmount;                       // continuous tangential flow shimmer   vec3 exDir = normalize(rad + vec3(sin(aSeed * 41.0), cos(aSeed * 57.0), sin(aSeed * 73.0)) * 0.45);   p += exDir * uExplode * uExplodeDist;             // drives BOTH the assembly and the exit burst   float period = mix(3.0, 9.0, aSeed);   float firePhase = aSeed * period;   float ft = mod(iTime + firePhase, period);   float fire = pow(clamp(1.0 - ft / 0.4, 0.0, 1.0), 2.5);   if (aSeed > uSynapseRate) fire = 0.0;             // only 10% of points ever fire   vSynapse = fire;   vec4 mv = modelViewMatrix * vec4(p, 1.0);   vec4 centerMv = modelViewMatrix * vec4(0.0, 0.0, 0.0, 1.0);   float rel = centerMv.z - mv.z;   vFrontness = clamp(rel * 0.6 + 0.5, 0.0, 1.0);   gl_Position = projectionMatrix * mv;   vec4 centerClip = projectionMatrix * centerMv;   vec2 centerNDC = centerClip.xy / max(0.0001, centerClip.w);   vec2 pNDC = gl_Position.xy / max(0.0001, gl_Position.w);   float screenDist = length(pNDC - centerNDC);   vCenterness = 1.0 - clamp(screenDist / max(0.05, uCenterRadius), 0.0, 1.0);   vec2 dMouse = pNDC - uMouse; dMouse.x *= uAspect;   vCursor = (1.0 - smoothstep(0.0, uCursorRadius, length(dMouse))) * uCursor;   float baseSize = uSize * (iResolutionY / 720.0) * (200.0 / -mv.z);   gl_PointSize = baseSize * (1.0 + fire * 0.8 + vHighlight * 1.8 + vCursor * 1.3);   vDepth = -mv.z; } ```  **Fragment shader** (verbatim):  ```glsl uniform vec3 uCool; uniform vec3 uWarm; uniform vec3 uEdgeColor; uniform vec3 uCenterColor; uniform float uCenterFalloff; uniform vec3 uSynapse; uniform float iAlpha; uniform float uGlow; uniform float uDepthDarkness; uniform vec3 uDeepColor; uniform float uOcclusionStrength; uniform vec3 uHighlightColor; uniform float uHighlightStrength; uniform float uFocusFadeStrength; uniform float uIsolateStrength; uniform float uExplode; uniform vec3 uCursorColor; varying float vSeed; varying float vSynapse; varying float vHemi; varying float vDepth; varying float vFrontness; varying float vCenterness; varying float vOcclusion; varying float vHighlight; varying float vFar; varying float vCursor; varying vec3 vWorldPos; void main() {   vec2 p = gl_PointCoord - 0.5;   float r = length(p);   if (r > 0.5) discard;   float core = pow(smoothstep(0.5, 0.0, r), 2.2);   float t = pow(vCenterness, max(0.05, uCenterFalloff));   float vt = clamp(smoothstep(-0.7, 0.9, vWorldPos.y) + vSeed * 0.12, 0.0, 1.0);   vec3 grad = mix(uCool, uWarm, vt);              // full vertical gradient, like the hero orb   vec3 base = mix(grad, uCenterColor, t);         // fades toward the dark interior   base = mix(base, uEdgeColor, (1.0 - t) * 0.18); // outer rim lifted with the edge colour   base = mix(base, uDeepColor, clamp(vOcclusion * uOcclusionStrength, 0.0, 1.0));   vec3 col = base + uSynapse * vSynapse * 2.0;   col = mix(col, uHighlightColor, vHighlight * 0.5);   col += uHighlightColor * vHighlight * 0.7;   float nonFocus = (1.0 - vHighlight) * uHighlightStrength;   col = mix(col, uDeepColor, nonFocus * uIsolateStrength);   float depthMul = mix(1.0 - uDepthDarkness, 1.0, vFrontness);   col *= depthMul;   float alphaOut = core * iAlpha * mix(1.0 - uDepthDarkness * 0.7, 1.0, vFrontness);   alphaOut *= 1.0 + vHighlight * 0.8;   float focusDim = 1.0 - uHighlightStrength * uFocusFadeStrength * vFar;   col *= focusDim; alphaOut *= focusDim;   col += uCursorColor * vCursor * 0.8;   alphaOut += vCursor * core * 0.32;   alphaOut *= 1.0 - smoothstep(0.0, 1.0, uExplode) * 0.8;   gl_FragColor = vec4(col * uGlow, alphaOut); } ```  **Per frame:** ```js const appear = brainAppear(clock); const exit   = smoothstep(0.12, 1, outro);       // dead-zone, then a soft burst iTime   = elapsed; iAlpha  = appear * (1 - smoothstep(0.55, 1, outro));   // hold brightness through the approach uExplode = Math.max(1 - appear, exit);                 // 1→0 assembling, 0→1 bursting group.position.z = exit * 2.5;                         // rushes the camera on the way out iResolutionY = height * dpr; uAspect = width / height; uCursorRadius = clamp(0.1 * 4.6 / camera.position.length(), 0.04, 0.13); uMouse.x += (pointer.x - uMouse.x) * 0.18;  uMouse.y += (pointer.y - uMouse.y) * 0.18; uCursor += ((pointerReaction ? 1 : 0) - uCursor) * 0.1; // The spin overlaps the assembly and runs right up to the burst — one continuous move. const spin = clamp01((clock - 2.7) / 1.3) * 2.4;                       // scrollSpin targetRy = clamp(pointer.x,-1,1) * 0.22 * uCursor;                     // cursorTilt targetRx = -clamp(pointer.y,-1,1) * 0.22 * 0.65 * uCursor; parallax.ry += (targetRy - parallax.ry) * 0.05;  parallax.rx += (targetRx - parallax.rx) * 0.05; group.rotation.y = -1.309 - 1.5708 + spin + parallax.ry;   // baseRotationY — a lateral profile group.rotation.x = parallax.rx; ``` Visible while `appear > 0.001 && outro < 0.998`. Static uniforms: `uHighlightStrength: 0`, `uOcclusionStrength: 0`, `uHighlightRadius: 0.6`, `uFocusFadeStrength: 0.55`, `uIsolateStrength: 0.88`, `uHighlightPos: (0,0,0)`, `uHighlightColor: #2563eb`, `uExplodeDist: 5`.  ### 6. Atmosphere — camera-attached motes  `atmoCount` points at random positions in `[-1,1]³`, each with a `size` attribute of `atmoSize * (0.4 + Math.random())`. The whole `Points` object **follows the camera** (`points.position.copy(camera.position)`) — the camera travels from z≈4 to z≈48, so nothing anchored to the world would survive the dive.  ```glsl // vertex attribute float size; uniform float uTime; uniform vec2 uRes; uniform float uSpread, uFadeNear, uFadeFar; varying float vA; vec3 warp(vec3 p, float t){   float c = 0.9, a = 1.9, b = 0.02, s = 0.05;   p *= 2.0;   p.x += c*sin(s*t + a*p.y) + t*b; p.y += c*cos(s*t + a*p.x);   p.y += c*sin(s*t + a*p.z) + t*b; p.z += c*cos(s*t + a*p.y);   p.z += c*sin(s*t + a*p.x) + t*b; p.x += c*cos(s*t + a*p.z);   return cos(p + vec3(1, 2, 4)); } void main(){   vec3 v = position * uSpread + warp(position, uTime) * (uSpread * 0.28);   vec4 mv = modelViewMatrix * vec4(v, 1.0);   float r = length(v);   float farF = 1.0 - smoothstep(uFadeNear, uFadeFar, r);   float nearF = smoothstep(0.0, 0.3, -mv.z);   vA = farF * nearF;   gl_PointSize = size * uRes.y / 900.0 / -mv.z;   gl_PointSize = max(gl_PointSize, 1.0);   gl_Position = projectionMatrix * mv; } ``` ```glsl // fragment uniform vec3 uColor; uniform float uAlpha; varying float vA; void main(){   vec2 p = gl_PointCoord - 0.5;   float l = length(p);   if (l > 0.5) discard;   float tex = smoothstep(0.5, 0.0, l);   gl_FragColor = vec4(uColor * tex, tex * vA * uAlpha); } ``` `uTime = elapsed * 8`. Everything else **crossfades on `galaxyPresence`**:  | uniform | orb | galaxy | |---|---|---| | `uColor` | `#c9a8ff` | `#c9a8ff` | | `uAlpha` | `0.5` | `0.6` | | `uSpread` | `2.2` | `4.0` | | `uFadeNear` | `2.0` | `5.0` | | `uFadeFar` | `3.2` | `6.5` |  ### 7. Camera rig  One shared camera travels between the forms rather than rescaling them — the orb is authored close (z 3.8), the galaxy far out (z 48, diving to 18):  ```js const presence = galaxyPresence(state); const eased = 1 - Math.pow(1 - intro, 2); const galaxyZ = 48 - state.galaxyDive * 30;                     // cameraZ - dive const targetZ = lerp(3.8, galaxyZ, presence) + (1 - eased) * 10;  // + INTRO_DOLLY, pulled back at start camera.position.z += (targetZ - camera.position.z) * Math.min(1, delta * 8);  const sway = lerp(0.35, 4, presence);        // ORB_PARALLAX → GALAXY parallax const targetX = pointer.x * sway * eased; const targetY = pointer.y * sway * 0.55 * eased; const follow = Math.min(1, delta * 3); camera.position.x += (targetX - camera.position.x) * follow; camera.position.y += (targetY - camera.position.y) * follow; camera.lookAt(0, 0, 0); ``` Because `presence` returns to 0 as the galaxy disperses, the camera pulls back to the orb's close distance exactly as the brain forms there.  ### 8. Bloom  `EffectComposer` → `RenderPass` → `UnrealBloomPass`, with **strength 0.6, radius 0.3, threshold 0.6**. The threshold is in **linear** light (colours are uploaded linear), so a low threshold catches the background wash and smears the whole frame — only genuinely hot cores should bloom. Skip the composer entirely on the weakest tier (see the table) and render straight to screen.  ## The loader  A black full-screen curtain (`position:fixed; inset:0; z-index:60; background:#000`), Mulish, uppercase, `1.111vw`, colour `--chalk`. It holds:  - **Top-left:** `VESPER` and, at 35% opacity, `V—0RB / SYS.04`. Padding `2rem` / `1.5rem`. - **Bottom:** a row with `INITIALIZING ENVIRONMENT` (0.972vw, 40% opacity) on the left and the counter   on the right — `String(percent).padStart(3,'0')` in tabular nums at 80% opacity, then a `%` at 35%.   Under it, a **1px hairline** track (`bg: rgba(255,255,255,0.12)`) with a white bar filling to   `percent%` (transition 220 ms, easeOutCubic). - **Behind it all, a `<canvas>` star field** (below).  **Timing.** There is nothing real to load (the scene builds its own geometry, and the brain mesh is fetched, never blocked on), so the bar is driven by a floor: `percent = min(1, elapsed/900) * 100`. Once `elapsed >= 900 ms` and the bar reads 100, hold **260 ms** at full, then fly the curtain out over **900 ms** (`opacity 1→0`, `scale 1→1.06`, `cubic-bezier(0.6, 0.05, 0.2, 1)`), and **90 ms** after the curtain starts lifting, start the intro clock. Remove the curtain from the DOM when it finishes.  **The star field** — a 2D canvas of **140** falling streaks:  ```js const ANGLE = -0.32;                       // ~18°, leaning left as they fall const dx = Math.sin(ANGLE), dy = Math.cos(ANGLE); // Each star: x, y, z (depth 0..1), seed, and an RGB sampled from the ORB GRADIENT by seed: //   stops: #52ffa5 (mint) → #6bfdff (cyan) → #582eff (violet)  — the hero orb's own colours // Spawn above the frame, offset so the angled drift still covers it: //   x = random()*(w + h*|dx|) - h*|dx|*0.5;  y = initial ? random()*h : -random()*h*0.4 - 40 ``` Per frame, with `boost = intensity()`: ```js // Trails are drawn by FADING, not clearing — the fade weakens as boost grows, lengthening the trails. ctx.globalCompositeOperation = 'source-over'; ctx.fillStyle = `rgba(0, 0, 0, ${0.34 - Math.min(boost, 2) * 0.1})`; ctx.fillRect(0, 0, w, h); ctx.globalCompositeOperation = 'lighter'; for (const s of stars) {   const depth = 0.25 + s.z * 0.75;   const speed = (140 + depth * 620) * (0.55 + boost * 1.35);   const step  = speed * delta;   const px = s.x, py = s.y;   s.x += dx * step; s.y += dy * step;   const length = step * (1.1 + boost * 1.6);   const tailX = px - dx * length, tailY = py - dy * length;   const alpha = (0.10 + depth * 0.5) * Math.min(1, 0.35 + boost);   // Trail fades out in the star's own colour; the head pushes 30% toward white — a hot comet tip.   const grad = ctx.createLinearGradient(tailX, tailY, s.x, s.y);   grad.addColorStop(0, `rgba(${s.r},${s.g},${s.b},0)`);   grad.addColorStop(1, `rgba(${hr},${hg},${hb},${alpha})`);   ctx.strokeStyle = grad; ctx.lineWidth = 0.5 + depth * 1.4; ctx.lineCap = 'round';   ctx.beginPath(); ctx.moveTo(tailX, tailY); ctx.lineTo(s.x, s.y); ctx.stroke();   if (s.y - length > h || s.x < -h || s.x > w + h) respawn(s); } ``` **`intensity`** is `percent/100` while loading (the field accelerates with the bar), then on exit it ramps `1 → 1 + 2.6` across the 900 ms curtain — the streaks lengthen and accelerate into **warp** as the curtain leaves. Read it every frame from a mutable ref, never through state.  ## Layout & sections (in order)  ### Fixed header (always on top, `z-index:50`)  A centred glass pill: `position:fixed; top:0.694vw; left:50%; translateX(-50%); height:3.542vw; width:49.514vw; max-width:95vw; border:1px solid rgba(255,255,255,0.1); background:rgba(0,0,0,0.8); backdrop-filter:blur(8px)`. Inside, a `justify-between` row (`padding:0.556vw`, `padding-left:0.833vw`): the **logo** (`logo.svg`, `1.806vw × 5.86vw`), the nav — **Home · Services · Works · About** (General Sans 400, `1.111vw`, gap `2.222vw`) — and a bordered **Contact Us** button (`self-stretch`, `padding-inline:1.111vw`, with a `0.139vw` square dot after the label).  Below 1024px: the pill keeps the logo, hides the links, and shows a burger that opens a full-screen overlay menu. `top:1rem; height:3.5rem; width:calc(100% - 3rem)`.  ### Hero overlay (fixed, `z-index:10`, `pointer-events:none`)  Fades out as the scene scrolls past section one: `opacity = 1 - clamp01((clock - 0.08) / 0.2)`. Copy reveals **once**, gated on the intro (i.e. after the loader hands off), with these delays:  - **`h1` — "Motion instead of chrome"** — `LETTER_REVEAL`, delay 200 ms. Absolute at   `top:9.028vw; left:1.667vw; width:41.875vw`, General Sans 300, `5.556vw`/`0.9`. - **Bottom cluster** — absolute `left/right:1.667vw; bottom:2.153vw; height:14.514vw`:   - **Tagline** (`WORD_REVEAL`, delay 500) at `top:0; left:0; width:27.5vw`, Mulish uppercase     `1.111vw`: *"It reads your presence — pointer, scroll, dwell — and it answers in motion."*   - **Support** (`WORD_REVEAL`, delay 560) at `top:0; right:0; width:19.348vw`, General Sans `1.111vw`:     *"A rendering and interaction layer that turns your presence into motion, tuned to feel alive under     every pointer."*   - **Tag row** (`UNIT_REVEAL`, delay 700) at `top:13.194vw; left:0`, Mulish uppercase `1.111vw`, gap     `1.667vw`, with a `0.208vw` white square between items:     `[ LIVING INTERFACE ]` · `[ MOTION LAYER ]` · `[ MOTION LAYER ]`   - **Send Request CTA** (`UNIT_REVEAL`, delay 820) at `top:10.972vw; right:0`, `pointer-events:auto`.  **The Send Request CTA** (shared with the brain overlay): a link containing a filled block — `background:#fdfdfd; color:#000; padding:1.111vw 4.167vw`, General Sans 500 `1.111vw` — and, beside it (gap `0.278vw`), the `send-icon.svg` tile at `3.542vw`. Hover springs the block to the signal mint `#6cf3a3`, press to `#4bd888`. **No scale on hover, anywhere on this page** — controls answer in colour and weight only.  Below 1024px the whole overlay becomes a plain top-to-bottom flex stack in `rem` (`padding:6.5rem 1.5rem 2rem`), title `3.25rem`, body `0.9375rem`.  ### Galaxy overlay (fixed, `z-index:10`)  `opacity = smoothstep(0.32, 0.52, clock) * (1 - smoothstep(1.9, 2.3, clock))`. Copy reveals in **`always`** mode while `clock > 0.45 && clock < 2.05` (and reverses out after).  - **`h2` — "Reads presence, in motion"** — `LETTER_REVEAL`, centred: `top:7.014vw; left:50%;   translateX(-50%); width:46.319vw; text-align:center` (the split container is flex, so it also needs   `justify-content:center`). - **Bottom cluster** (`inset-x:0; bottom:0; height:16.181vw`):   - **Support copy** (`WORD_REVEAL`, delay 200), centred, `top:0; width:29.097vw`, General Sans     `1.111vw`: *"Every particle here is a reply to you — pointer, scroll and dwell, read live and     answered as pure, drawn motion."*   - A **rule** across the band at `top:4.792vw` (`border-top:1px solid rgba(255,255,255,0.2)`), and     three **column dividers** at `left: 25vw / 50vw / 75vw` (`height:11.389vw`, same 20% white).   - Four **stats** at `top:8.333vw`, each `width:11.111vw`, `border-left:1px solid rgba(255,255,255,0.8)`,     `padding-left:1.111vw`, stacked (`gap:0.833vw`): value in Mulish 300 `2.222vw`, label in Mulish 400     `1.111vw`. Left offsets and content:      | left | value | label |     |---|---|---|     | `6.944vw` | `91k` | Live particles |     | `31.944vw` | `8.3` | Frame budget ms |     | `56.944vw` | `0.9` | Cold start secs |     | `81.944vw` | `60` | Frames per sec |  Below 1024px: the copy stacks and the stat band becomes a 2-column grid (1 column on phones) under a single top border; the desktop rule and dividers are hidden.  ### Brain overlay (fixed, `z-index:10`)  `opacity = smoothstep(2.2, 2.65, clock) * (1 - smoothstep(3.6, 4.1, clock))`. Copy reveals in **`always`** mode while `clock > 2.55 && clock < 3.7`. A four-corner composition that brings the two previous titles together:  - **Top-left `h2`** — *"Motion instead of chrome"* (`LETTER_REVEAL`), `top:9.028vw; left:1.667vw;   width:41.875vw`, `5.556vw`/`0.9`. - **Bottom-right `h2`** — *"Reads presence, in motion"* (`LETTER_REVEAL`, delay 120), `right:1.667vw;   bottom:1.667vw; width:40.972vw; text-align:right` (flex container: `justify-content:flex-end`). - **Left tagline** (`WORD_REVEAL`, delay 200) at `top:calc(50% + 2.361vw); left:1.667vw; width:26.597vw`,   Mulish uppercase: *"Point counts, tessellation and pixel ratio step down by device, never by frame   rate."* - **Right tagline** (`WORD_REVEAL`, delay 260) at `top:calc(50% + 2.361vw); right:1.667vw;   width:14.583vw; text-align:right`, Mulish uppercase: *"Reduced-motion honoured across the page"*. - **Send Request CTA** (`UNIT_REVEAL`, delay 500) at `bottom:1.667vw; left:1.667vw`, `pointer-events:auto`.  Below 1024px the four corners collapse into one centred column.  ### "A living interface" — the white card (in flow, `z-index:10`)  `section`: `margin:0 auto; height:44.167vw; width:96.667vw; perspective:1400px`. Inside, the card: `height:100%; width:100%; transform-origin:bottom; border:1px solid rgba(255,255,255,0.1); background:#fff; color:#000` — inset from the viewport edges **on purpose**, so the live shader stays visible around it as the card floats up. It plays `CARD_REVEAL` in `always` mode (reverses as it leaves), and its copy reveals over it:  - **`h2` "A living interface"** — `LETTER_REVEAL`, `top:2.222vw; left:2.222vw; width:41.875vw`,   `5.556vw`/`0.9`. - **Photo** — `MASK_REVEAL` (unmasks top→bottom), `top:2.222vw; right:2.222vw; height:39.722vw;   width:27.361vw`, `object-fit:cover`. `financial.png`, alt *"A figure lit in teal, face turned toward   the light"*. - **`[ 01 ]`** (`UNIT_REVEAL`) at `top:34.236vw; left:2.222vw`, and under it (`top:36.667vw`, same left,   `width:20.347vw`) body one (`WORD_REVEAL`, delay 120):   *"Vesper is a rendering and interaction layer that reads presence — pointer, scroll and dwell — and   answers in motion rather than in chrome around it."* - **`[ 02 ]`** at `top:34.236vw; left:24.792vw`, and under it (`top:36.667vw`, `width:20.347vw`) body two   (`WORD_REVEAL`, delay 180):   *"Point counts, tessellation and device pixel ratio all step down by viewport class, and the render   loop yields sooner rather than starving the frame."*  The card's background is **opaque** — do not put a backdrop-blur on it; it would be invisible and cost a full-card filter pass every frame over a live WebGL canvas.  Below 1024px: `height:auto; width:calc(100% - 3rem)`, a padded stack (`1.5rem`), photo `20rem` tall.  ### "Frequently asked" — the second white card  Same treatment (`width:96.667vw; perspective:1400px`; card `padding:2.222vw 0 3.056vw`, centred column). Sits right under the first card (they stack close together).  - **An asterisk mark** (`asterisk.svg`, `1.806vw`) that **spins 360° clockwise on a 9 s loop** —   gate the loop on in-view, or it keeps an animation alive for the entire page to turn an 18px glyph   nobody can see. - **`h2` "Frequently asked"** — `LETTER_REVEAL`, centred, `margin-top:1.111vw`, `5.556vw`/`0.9`. - **The accordion** (`UNIT_REVEAL`) — `margin-top:4.167vw; width:42.778vw`, `border-top:1px solid   rgba(0,0,0,0.15)`, each row `border-bottom` the same. Row button: full width,   `padding:1.667vw 0.75vw`, question in Mulish uppercase `1.111vw`, and a **`+` marker**   (`1.389vw`, two 1px black bars) that **rotates 45° into an ×** when open. The whole row is the   control (hover tints it `rgba(0,0,0,0.035)`). The panel's height animates from 0 to its measured   `scrollHeight` (measure before paint, and re-measure on resize, so the first open never flashes);   the answer is General Sans `0.972vw`/`1.4`, `width:33.958vw`. **The first item starts open.**  Items (verbatim):  1. **What exactly is Vesper?** — "A rendering and interaction layer. It reads presence — pointer,    scroll, dwell — and answers in motion rather than in chrome. The particle work you just scrolled    through is the engine talking about itself." 2. **Does it run on low-end hardware?** — "Yes. Point counts, tessellation, pointer reactivity and    device pixel ratio all step down by viewport class, and the render loop yields rather than starving    the frame. On integrated graphics it drops geometry before it drops frames." 3. **Is any of this accessible?** — "The scene is decorative and marked aria-hidden; every word on the    page is real text in the DOM. Reduced-motion is honoured globally — springs resolve instantly and    the particle field stops." 4. **Can I use it in my own product?** — "Not yet. Vesper is in residency while the engine settles.    Request access and we will write when the surface stops moving."  ### Footer — transparent, over the live shader  No opaque surface: the scene keeps drawing behind it. `padding-top:4.444vw`, white text.  - **`h2` "Let's talk."** — `LETTER_REVEAL` (`once`), centred, `5.556vw`/`0.9`. - **Contact pill** — `margin:2.917vw auto 0; width:38.264vw`. A glass row like the header:   `height:3.542vw; border:1px solid rgba(255,255,255,0.2); background:rgba(0,0,0,0.8);   backdrop-filter:blur(8px); padding:0.556vw; padding-left:1.667vw`. Two transparent inputs — **Name**   and **Email** (`width:12.5vw` each, white placeholder) — and a bordered **Contact Us** submit pushed   right (`margin-left:auto; self-stretch; padding-inline:1.111vw`), which reads in the **signal mint**   and hovers to a mint border + `rgba(108,243,163,0.12)` fill. Submit is a stub.   Focus rings: `outline:2px solid var(--signal); outline-offset:4px` — **not** animated (a focus   indicator that eases in is a focus indicator that is briefly wrong). - A **full-width divider** (`margin-top:4.444vw`, `border-top:1px solid rgba(255,255,255,0.15)`). - A `justify-between` row (`margin-top:2.014vw; width:96.667vw; padding-bottom:4.444vw`): on the left the   **logo** (`1.806vw × 5.86vw`) over the tagline in Mulish uppercase `1.111vw` — *"A living interface   that reads your presence and answers in motion, not chrome."* — and on the right three link columns   (`gap:6.25vw`, each `width:12.5vw`, `gap:1.667vw`), headings in Mulish uppercase, links in General   Sans `1.111vw` at 70% white, hovering to 100%:    | Engine | Studio | Connect |   |---|---|---|   | Overview | Origin story | Get in touch |   | The renderer | The team | Privacy |   | Interaction | Journal | Terms |   | Performance | In residency | Report an issue |   | Roadmap | | |  Below 1024px the contact pill becomes a stacked form (two inputs over a full-width button) and the columns become a 2-column grid under the wordmark.  ## Fixed parameters (bake these in)  **Orb** — `colorTop:#52ffa5`, `colorBottom:#582eff`, `colorEdge:#582eff`, `deform:0.135`, `brightness:1.24`, `opacity:1`, `spin:0.17`, `tilt:0.39`, `pointerRadius:1.76`, `oilBulge:0.46`, `oilRipple:0.34`, `oilDrag:0.95`, `rippleFreq:11`, `rippleSpeed:4`, `iridescence:0.6`, `radius:1`, `approach:2.6`, `distortGain:3.4`.  **Galaxy** — `colorEdge:#52ffa5` (arms), `colorCore:#582eff`, `opacity:0.55` (× the duplicate factor), `pointSize:6`, `brightness:1.02`, `armSpin:0.4`, `tilt:-0.5`, `scale:0.18`, `cameraZ:48`, `dive:30`, `diveTilt:0.5`, `parallax:4`, `pointerRadius:5`, `pointerStrength:2`.  **Brain** — `colorCool:#582eff` (bottom), `colorWarm:#52ffa5` (top), `colorEdge:#582eff`, `colorCenter:#000000`, `colorSynapse:#eafff8`, `colorDeep:#02040e`, `colorCursor:#6bfdff`, `centerRadius:0.37`, `centerFalloff:4`, `size:0.067`, `synapseRate:0.1`, `flowSpeed:2.3`, `flowAmount:0.025`, `glow:1.4`, `depthDarkness:1`, `radius:1.15`, `cursorTilt:0.22`, `scrollSpin:2.4`, `baseRotationY: -1.309 - 1.5708`, `approach:2.5`.  **Atmosphere/backdrop** — orb: `flameA:#8b98fe`, `flameB:#6bfdff`, `flameAmt:0.5`, `atmo:#c9a8ff`, `atmoSpread:2.2`, `atmoFadeNear:2.0`, `atmoFadeFar:3.2`, `atmoAlpha:0.5`. Galaxy: same colours, `flameAmt:0.195`, `atmoSpread:4.0`, `atmoFadeNear:5.0`, `atmoFadeFar:6.5`, `atmoAlpha:0.6`.  **Camera** — `fov:50`, `near:0.1`, `far:400`, orb distance `3.8`, `INTRO_DOLLY:10`, `ORB_PARALLAX:0.35`.  **Loader** — `MIN_DURATION_MS:900`, `HOLD_AT_FULL_MS:260`, `INTRO_DELAY_MS:90`, `EXIT_MS:900`, `EXIT_INTENSITY:2.6`, `STAR_COUNT:140`, `ANGLE:-0.32`. **Intro** — `2900 ms`, easeOutCubic. **Scroll** — `SCROLL_TRACK_LVH:200`.  **Adaptive tiers** — resolve **once on mount** from `window.innerWidth`; the last row the width fits under wins. Point sprites are fill-rate bound (cost ∝ `count × pointSize² × dpr²`), so these are the whole performance story:  | ≤ width | orbCount | orbPointSize | orbCameraZ | orbMoveY | galaxy segs (w×h) | brainCount | atmoCount | atmoSize | pointer | dpr | fps | progressLerp | bloom | |---|---|---|---|---|---|---|---|---|---|---|---|---|---| | ∞ (desktop) | 23000 | 28 | 3.8 | 0.8 | 160 × 420 | 130000 | 260 | 23 | on | 1–2 | 60 | 0.07 | on | | 1440 | 17000 | 27 | 3.8 | 0.8 | 130 × 330 | 90000 | 210 | 22 | on | 1–1.75 | 60 | 0.08 | on | | 1024 | 11000 | 24 | 4.5 | 0.9 | 100 × 250 | 50000 | 150 | 20 | **off** | 0.9–1.25 | 45 | 0.13 | on | | 640 | 6500 | 21 | 5.4 | 1.0 | 70 × 170 | 26000 | 90 | 18 | **off** | 0.75–1.1 | 30 | 0.15 | **off** |  Throttle the render loop to the tier's fps by skipping whole rAF frames, skip work entirely when `document.hidden`, and honour `prefers-reduced-motion` (resolve reveals instantly; stop the particle field).  ## Assets  `ASSET_BASE_URL = https://api.getlayers.ai/storage/v1/object/public/public/assets/vesper-5b73726b62`  | file | full URL | used by | |---|---|---| | `brain/brain-mesh.bin` | `…/vesper-5b73726b62/brain/brain-mesh.bin` | the brain point cloud (VBRN container, 43 KB) | | `hero/logo.svg` | `…/vesper-5b73726b62/hero/logo.svg` | header + footer wordmark | | `hero/send-icon.svg` | `…/vesper-5b73726b62/hero/send-icon.svg` | the Send Request arrow tile | | `sections/asterisk.svg` | `…/vesper-5b73726b62/sections/asterisk.svg` | the spinning FAQ mark | | `sections/financial.png` | `…/vesper-5b73726b62/sections/financial.png` | the photo on the "A living interface" card |  Fetch the brain mesh with `fetch(url).then(r => r.arrayBuffer())` and decode it as above. Load the SVGs and the PNG as plain `<img src>`.  ## Done when  Loading shows a black curtain with mint/cyan/violet streaks accelerating into warp behind a `000%` → `100%` hairline counter; the curtain lifts and a mint-to-violet point orb assembles out of points streaming past the camera, breathing and bulging under the cursor, under "Motion instead of chrome". Scrolling scatters it into liquid sheets that gather into a spiral galaxy while the camera dives into the core; the galaxy detonates and its dust converges into a spinning point-cloud brain with firing synapses; the brain rushes forward and bursts as a white "A living interface" card floats up over the still-drawing shader, followed by the FAQ accordion and a transparent footer — with the scene visible around all of them.

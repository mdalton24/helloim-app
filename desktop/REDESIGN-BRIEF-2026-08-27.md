# Redesign brief — pasted by Mark, 2026-08-27 00:1x

Kept verbatim. His words are the spec; anything below that turns out to be
wrong about our own files is a note added underneath, never an edit to his text.

**ONE CORRECTION FLAGGED TO HIM AND NOT ASSUMED:** the brief says "noname.ai"
throughout. The product is **NameOS**, at **nameos.ai**. Every file path and
every finding in it maps to our real code, so it reads as a slip in a generated
document rather than a rename — but a rename would be a far bigger job than
this brief describes, so it was put to him rather than quietly corrected.

**IT ANSWERS THE TWO OPEN DECISIONS** from the findings email: the accent is the
site's mint/cyan, and yes to real brand logos.


## LOCKED DECISION — the ring stays

Mark, 2026-08-27, after reading the brief back: **"I dont want to retire the
ring."**

His own pasted brief said *"retire the static, oversized particle ring that
consumes 85% of viewport real estate"* — and he has overruled it. That line is
dead. **Do not re-propose retiring, shrinking or replacing the ring**, and do
not reintroduce it from the brief text above, which is kept verbatim on purpose
and is not the current instruction.

The observation underneath it was real — most of the window carries no
information — but the fix has to be additive. Anything that reclaims that space
does it AROUND the ring, never instead of it.
